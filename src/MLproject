name: apple_project

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      data_path: {type: str, default: "../data/fake_data.csv"}
    command: "python3 05_mlflow_experiment_mlproject.py --data_path {data_path}"